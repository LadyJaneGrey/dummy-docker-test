name: Hello World Workflow

on:
  push:
    branches:
      - main

jobs:
  hello-world-job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Dummy Build Application 
      run: echo "Hello, Vincent"

